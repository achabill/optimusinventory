'use strict';

optimusInventoryApp
    .factory('LogsService', ['$http', "UserService", function ($http, userService) {
        //var baseEndPoint = window.location.origin + "/api/users";
        //var baseEndPoint = "http://localhost:8080/api/users";

        var service = {};
        return service;
    }]);